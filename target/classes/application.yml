spring:
  datasource:
    url: jdbc:mysql://localhost:3306/lms?useSSL=false
    username: root
    password: admin123
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    show-sql: true